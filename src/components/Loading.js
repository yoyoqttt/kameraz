import React from 'react';

const Loading = ({message}) => {
    return (
       <div className="loading-container">
         <p className="loading-message"> cvbn</p>
      </div>
    );
}

export default Loading;